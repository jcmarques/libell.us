# Dependencies
node_modules

# Build output
dist

# Environment variables (may contain API keys, secrets)
.env
.env.local
.env.*.local

# Editor / IDE
.DS_Store
*.local

# MCP / local tool config (may contain secrets)
.cursor/mcp.json

# Cursor local tasks (not tracked)
.cursor/website-tasks.md

# Logs
*.log
npm-debug.log*

# Vite cache
.vite
